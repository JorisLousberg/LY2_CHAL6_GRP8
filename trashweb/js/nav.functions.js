function changeTextColor(element) {
    var text = document.getElementById(element);

    text.style.color = "black";
}

function restoreTextColor(element) {
    var text = document.getElementById(element);

    text.style.color = "#8ebf42";
}